ћассив (Array) - это набор переменных одного типа в непрерывной области пам€ти.


		type name[SIZE];
type - тип элементов массива;
name - им€ массива;
SIZE - количество элементов массива

		const int SIZE = 5;
		int arr[SIZE];